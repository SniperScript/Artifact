name: Artifact
description: "Your college mate"
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # ... [Thy other existing packages] ...
  shared_preferences: ^2.2.3
  google_fonts: ^6.2.1
  table_calendar: ^3.2.0
  flutter_local_notifications: ^22.2.0
  timezone: ^0.11.1

  # THE GOOGLE NETWORK PROTOCOLS
  google_sign_in: ^6.2.1
  googleapis: ^13.1.0
  http: ^1.6.0
  intl: ^0.20.3
  flutter_staggered_grid_view: ^0.7.0
  share_plus: ^13.3.0
  just_audio: ^0.9.36
  just_audio_background: ^0.0.1-beta.11
  audio_session: ^0.1.18
  dartssh2: ^2.22.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/
